'use strict';

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { 'default': obj }; }

var _sinon = require('sinon');

var _sinon2 = _interopRequireDefault(_sinon);

var _ = require('../');

describe('funcSubject()', function () {
  it('creates an observable function that pushes new values when called', function () {
    var v$ = _.funcSubject();
    var spy = _sinon2['default'].spy();
    var s = v$.subscribe(spy);

    v$(1);
    v$(2);
    v$(3);

    s.dispose();
    expect(spy.args.map(function (args) {
      return args[0];
    })).to.deep.equal([1, 2, 3]);
  });
});