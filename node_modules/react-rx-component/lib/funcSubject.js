'use strict';

exports.__esModule = true;
exports['default'] = funcSubject;

var _rx = require('rx');

// Idea and implementation borrowed from
// https://github.com/fdecampredon/rx-react

function funcSubject() {
  function subject(value) {
    subject.onNext(value);
  }

  /* eslint-disable */
  for (var key in _rx.Subject.prototype) {
    /* eslint-enable */
    subject[key] = _rx.Subject.prototype[key];
  }

  _rx.Subject.call(subject);

  return subject;
}

module.exports = exports['default'];